name: Vahan Dashboard Scraper PanIndia

on:
  schedule:
    # Optional: Automatically runs every day at midnight UTC (Change or remove if not needed)
    - cron: '0 0 * * *'
  workflow_dispatch:
    # Allows you to manually trigger the scraper from the GitHub UI

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 120 # Elevated timeout since your script processes 30+ states

    steps:
    - name: Checkout repository code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-node@v4 # Used implicitly by checkout for certain caching steps or setup tools, but let's use the explicit python setup below
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip' # Speeds up subsequent runs by caching packages

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install playwright

    - name: Ensure Playwright browsers and system dependencies are installed
      run: |
        playwright install chromium --with-deps

    - name: Run Web Scraper
      run: python3 main.py

    - name: Upload Scraped CSV Data as Artifact
      uses: actions/upload-artifact@v4
      if: always() # Forces artifact upload even if the script hits a timeout or minor crash midway
      with:
        name: vahan-scraped-data
        path: Panindia_*.csv
        retention-days: 14